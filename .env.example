# Production / custom API: set full base URL (no trailing slash).
# Example: VITE_API_URL=https://api.yourdomain.com
#
# Local dev: leave unset or empty to use Vite proxy to localhost:8080.
VITE_API_URL=https://extra-attendance-backend-production.up.railway.app
